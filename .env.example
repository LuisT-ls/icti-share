# ============================================
# CONFIGURAÇÃO DO BANCO DE DADOS
# ============================================
# URL de conexão com PostgreSQL
# Formato: postgresql://[user]:[password]@[host]:[port]/[database]?schema=public
# Exemplos:
#   - Local: postgresql://postgres:senha@localhost:5432/icti_share?schema=public
#   - Railway: postgresql://postgres:senha@containers-us-west-xxx.railway.app:5432/railway?schema=public
#   - Supabase: postgresql://postgres.xxx:senha@aws-0-us-west-1.pooler.supabase.com:6543/postgres?schema=public
#   - Neon: postgresql://user:senha@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://postgres:senha@localhost:5432/icti_share?schema=public"

# ============================================
# CONFIGURAÇÃO DO NEXTAUTH
# ============================================
# URL base da aplicação (sem barra no final)
# Em desenvolvimento: http://localhost:3000
# Em produção: https://seu-dominio.com
NEXTAUTH_URL="http://localhost:3000"

# Secret usado para criptografar tokens e sessões do NextAuth
# Gere um secret seguro usando: openssl rand -base64 32
# Ou use: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# ============================================
# ARMAZENAMENTO DE ARQUIVOS (UPLOADS)
# ============================================
# Caminho para armazenar os arquivos PDF enviados
# 
# Opções:
#   - Local: ./uploads ou ./public/uploads
#   - Railway: use RAILWAY_VOLUME_PATH (caminho persistente fornecido pelo Railway)
#   - Outros serviços: ajuste conforme o provedor
# 
# Exemplos:
#   - Desenvolvimento local: ./uploads
#   - Railway: /data/uploads (ou o caminho fornecido pelo Railway Volume)
#   - Vercel/Netlify: use serviço de storage (S3, Cloudinary, etc.)
UPLOAD_PATH="./uploads"

# Para Railway: caminho do volume persistente (opcional)
# Railway fornece volumes persistentes em /data por padrão
# RAILWAY_VOLUME_PATH="/data"

# ============================================
# CONFIGURAÇÃO DE E-MAIL (SMTP) - OPCIONAL
# ============================================
# Configurações para envio de e-mails (verificação, notificações, etc.)
# Se não configurado, funcionalidades de e-mail serão desabilitadas

# Servidor SMTP
# Exemplos:
#   - Gmail: smtp.gmail.com
#   - SendGrid: smtp.sendgrid.net
#   - Mailgun: smtp.mailgun.org
#   - Resend: smtp.resend.com
SMTP_HOST="smtp.gmail.com"

# Porta SMTP (geralmente 587 para TLS ou 465 para SSL)
SMTP_PORT=587

# Usuário/email para autenticação SMTP
SMTP_USER="seu-email@gmail.com"

# Senha ou app password do e-mail
# Para Gmail: use "App Password" (não a senha normal)
# Gere em: https://myaccount.google.com/apppasswords
SMTP_PASSWORD="sua-senha-ou-app-password"

# E-mail remetente (pode ser o mesmo do SMTP_USER)
SMTP_FROM="noreply@seu-dominio.com"

# Nome do remetente
SMTP_FROM_NAME="ICTI Share"

# Habilitar TLS/SSL (true para TLS na porta 587, false para SSL na porta 465)
SMTP_SECURE=false

# ============================================
# CONFIGURAÇÕES ADICIONAIS (OPCIONAL)
# ============================================
# Ambiente da aplicação
# Valores: development, production, test
NODE_ENV="development"

# Tamanho máximo de upload (em bytes)
# Padrão: 10MB = 10485760 bytes
MAX_UPLOAD_SIZE=10485760

# Tipos de arquivo permitidos (separados por vírgula)
ALLOWED_FILE_TYPES="application/pdf"
